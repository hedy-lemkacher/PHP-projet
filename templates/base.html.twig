<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Spektacles{% endblock %}</title>

    {# --- ✅ Tailwind via CDN (aucun build nécessaire) --- #}
    <script src="https://cdn.tailwindcss.com"></script>
    
    {# --- Phosphor Icons Duotone --- #}
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/@phosphor-icons/web@2.1.2/src/duotone/style.css">

    {# --- Google Fonts - Instrument Serif --- #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500;1,600;1,700&display=swap" rel="stylesheet">

    {# --- Font Aspekta --- #}
    <style>
        @font-face {
            font-family: 'Aspekta';
            src: url('/fonts/AspektaVF.woff2') format('woff2');
            font-weight: 100 900;
            font-style: normal;
            font-display: swap;
        }
        html {
            scroll-behavior: smooth;
        }
        
        body {
            font-family: 'Aspekta', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        }
        
        .decoration-line {
            position: fixed;
            top: 0;
            width: 1px;
            height: 100vh;
            z-index: 0;
            pointer-events: none;
            background: #1D1C2B;
        }
        
        .decoration-line-left {
            left: 40px;
        }
        
        .decoration-line-right {
            right: 40px;
        }
    </style>

    {# --- Si plus tard tu utilises Webpack Encore, tu pourras remettre ceci : --- #}
    {# <link rel="stylesheet" href="{{ asset('build/app.css') }}"> #}
</head>
<body style="background-color: #131119; color: #FFFFFF; margin: 0; padding: 0; overflow-x: hidden; max-width: 100vw; position: relative;">
    {# Ligne décorative gauche #}
    <div class="decoration-line decoration-line-left"></div>
    
    {# Ligne décorative droite #}
    <div class="decoration-line decoration-line-right"></div>
    
    {% block body %}{% endblock %}
</body>
</html>
